[[experience]]
  role = "Technical Product Engineer – HCS Security"
  company = "Huawei"
  company_link = "https://www.huawei.com"
  period = "Sep 2025 – Present"
  country = "Algiers, DZ"
  description = "Product security on Huawei Cloud Stack (HCS) deployments."

[[experience]]
  role = "GENAI Platform Engineer (Freelance)"
  company = "Cinématique"
  company_link = "https://cinematique.co/"
  period = "Oct 2025 – Feb 2026"
  country = "Paris, FR (Remote)"
  description = """Built and maintained custom RunPod serverless worker images for ComfyUI-based image and video generation pipelines, running models including Wan2.2-Animate 14B, ViTPose, YOLOv10, and SeC-4B. Designed base Docker images that all other worker images base from, standardized CUDA setup, dependencies, and entrypoints. Automated model downloads from HuggingFace and CivitAI into RunPod network volumes, containers attach pre-loaded volumes instead of pulling models on every cold start. Tested and validated end-to-end inference pipelines covering pose estimation, video segmentation, VAE decode, and multi-LoRA video generation."""
[[experience]]
  role = "GENAI Developer → NetDevOps Engineer"
  company = "InfraXcode"
  company_link = "https://infraxcode.com/"
  period = "Sep 2023 – Mar 2025"
  country = "Paris, FR (Remote)"
  description = """
    - Built an LLM-backed network device emulator as part of an R&D product: instead of spinning up physical sandboxes, NetDevOps and dev teams launch stateful emulated devices, connect their management plane, and run IaC and config management on top --- focus on deployment logic, not device setup

    - The agent roleplays as a real device, tracking session state and grounding every response in vendor CLI docs and YANG specs via RAG + BM25. Wrapped it behind an SSH server and a RESTCONF server I wrote from scratch --- Ansible playbooks and Terraform providers connect to it with no changes. NetOps team ran their real scripts against it blind: \~80% output match vs a real device

    - Core engineering challenges: state persistence (evaluated local file storage vs Neo4j across sessions), context window overflow (added a summarization node to compress history without losing session state), response accuracy (prompt engineering and retrieval tuning), and latency (inherent to token generation --- documented and accepted as a tradeoff vs real hardware)

    - Second product on the same platform: a test case orchestrator. User specifies a script and a set of device states --- SSH port closed, port open with wrong credentials, missing config --- the orchestrator spins up an emulated device per state, runs the script, and returns results side by side. Lets engineers see exactly how their scripts fail before touching real devices

    - Moved off external LLMs (OpenAI, Groq) mid-project by deploying open-source fine-tuned models (Llama) locally via HuggingFace and Ollama --- removed API dependency and improved response latency
  """